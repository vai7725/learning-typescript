let greetings: string = "Hello vaibhav"

const num: number = 45

type User = {
    readonly _id: string;
    name: string;
    contact: number;
}

function createUser(user: User):User {
    return user
}

console.log(greetings);

export{}